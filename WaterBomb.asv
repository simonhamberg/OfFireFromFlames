function  coords = WaterBomb(range,target)
    coords = zeros(1,range);
    for i = 1:range 
        randomNr = rand;
        coords(1,i) = target - range * (2*randomNr-1);
        %coords(2,i) = target - range * (2*randomNr-1); 
    end
    
end